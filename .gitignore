node_modules/
.DS_Store
*.log
# NOTE: dist/ is committed — GitHub Actions runtime needs the prebuilt
# bundle to be present in the repo. The build is reproducible via
# `pnpm install && pnpm run build`.
